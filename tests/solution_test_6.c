#include <stdio.h>

int main()
{
    int a , b;
    scanf("%d%d", &a, &b);

    if ( a > 10 )
    {
        if ( b <= 90 )
        {
            printf("a: %d, b: %d\n",a,b);
        }
        else
        {
            printf("a: %d, b: %d\n",a,b);
        }
    }
    else
    {
        if ( b > 50 )
        {
            printf("a: %d, b: %d\n",a,b);
        }
        else
        {
            printf("a: %d, b: %d\n",a,b);
        }

    }

    if ( a < 30 )
    {
        if ( b <= 21 )
        {
            printf("a: %d, b: %d\n",a,b);
        }
        else
        {
            printf("a: %d, b: %d\n",a,b);
        }
    }
    else
    {
        if ( b > 11 )
        {
            printf("a: %d, b: %d\n",a,b);
        }
        else
        {
            printf("a: %d, b: %d\n",a,b);
        }

    }
    return 0;
}
