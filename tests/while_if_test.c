#include <stdio.h>

int main()
{
    int x , y, i;
    scanf("%d%d%d", &x, &y, &i);

    while ( i >= 0 )
    {
        if ( x > 10 )
        {
            puts("Test");
        }
        else
        {
            puts("Test");
        }
        if ( y <= 10 )
        {
            puts("Test");
        }
        else
        {
            puts("Test");
        }

        i--;

    }
    return 0;
}
